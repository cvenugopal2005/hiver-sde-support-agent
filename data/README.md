# Data

Raw data is **not** committed to this repository by default (see `.gitignore`).

- `data/raw/` — will hold the raw dataset subsample once it is downloaded.
- `data/processed/` — will hold any derived/cleaned data.

The dataset acquisition and sampling procedure (source, brand selection, subsample
size, filtering logic) has not been decided yet and will be documented here after
dataset exploration.
